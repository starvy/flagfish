// Player-surface helpers. submitFlag lives in the shared harness; re-exported here so specs written
// against the ../helpers path keep working against the one canonical implementation.
export { submitFlag } from "./fixtures";
